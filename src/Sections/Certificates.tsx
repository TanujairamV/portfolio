import React from "react";
const Certificates: React.FC = () => (
  <section style={{background: "#111", color: "#fff", padding: 20}}>
    <h2>Certificates DEBUG</h2>
    <div>This section is definitely rendering!</div>
  </section>
);
export default Certificates;
